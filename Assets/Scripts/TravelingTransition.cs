using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TravelingTransition : MonoBehaviour
{
    public bool transition;
    public bool stop = false;

    void Start()
    {
        
    }

    void Update()
    {
        //if(transition)
       // {
           // stop = true;
            //offsetDist -= (Time.deltaTime * speed) / 10;
           // if (offsetDist >= 1 || offsetDist <= -1)
           // {
               // urban.GetComponent<RectTransform>().anchoredPosition += new Vector2(Mathf.Round(offsetDist), 0);
                //mountains.GetComponent<RectTransform>().anchoredPosition += new Vector2(Mathf.Round(offsetDist), 0);
                //offsetDist -= Mathf.Round(offsetDist);
            //}
        //}
        
    }
}
